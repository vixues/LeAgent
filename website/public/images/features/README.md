# Feature screenshots

Place product screenshots here for the Introduction page (`/about`).

## Core features tab

| File | Scenario |
|------|----------|
| `chat.png` | Chat |
| `genui-weather.png` | Generative UI |
| `code-generation.png` | Code generation |
| `word-generation.png` | Word document generation |
| `webpage-generation.png` | Web page generation |
| `paper-mode.png` | Research Paper Mode |
| `multi-model.png` | Multi-model routing (used by the multi-model band) |

## Workflows tab

| File | Scenario |
|------|----------|
| `workflow-editor.png` | Visual DAG / node editor |
| `workflow-art.png` | Game-art asset pipeline |

> `workflow.png` and `workflow-run.png` are legacy placeholders and are not used on the site.

Recommended aspect ratio: 16:10 (e.g. 1280×800).

Bundled `*.png` files are placeholders (mock UI). Replace any file with a real
screenshot using the **same filename** — no code change required. The filename →
scenario mapping lives in [`src/lib/features.ts`](../../../src/lib/features.ts).
