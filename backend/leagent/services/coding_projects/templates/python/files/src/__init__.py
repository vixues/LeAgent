"""Python project scaffolded by LeAgent's coding agent.

Add your modules here and import them from ``main.py`` or other
entry points.  The package is already on ``sys.path`` so bare
``from src import …`` works everywhere.
"""
